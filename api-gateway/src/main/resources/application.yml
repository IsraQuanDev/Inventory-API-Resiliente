server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: inventory-service
              uri: http://localhost:8081
              predicates:
                - Path=/api/products/**
              filters:
                - CircuitBreaker=inventoryCircuitBreaker